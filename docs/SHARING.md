# Sharing the initial source version

Repository: https://github.com/orionarch26/orion-image-workbench

Use `docs/media/workbench-demo.png` for the public preview. It uses only the bundled synthetic cat example and clearly marked demonstration state. Do not reuse full screenshots of a personal gallery.

Suggested English post:

> I've published Orion Image Workbench: a local ComfyUI workbench for sequential image batches, reusable recipes and fixed-seed A/B experiments. The initial source version includes a verified model downloader and English/Chinese setup docs. Linux tested; Windows app support and full UI i18n are next.

Attach the repository link and preview. Split text into a short thread if needed by the account's post limits. This is a draft, not a posted announcement.

中文草稿：

> 我把自己的本地生图工作台整理成了开源项目：连续批量出图、固定种子比较参数、保存配方，以及断线后核对原任务。首版附中英安装文档和带校验/续传的模型下载器。Linux已实测，Windows应用与完整界面双语化继续推进。

For a video, show the real workflow: select a recipe, submit a small batch, choose a result, compare one parameter, and save a recipe. Label accelerated waiting segments. Describe model licensing separately from the application license, and state the actual GPU/model configuration for any performance numbers.
